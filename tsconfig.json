{
  "files": [], // No specific files for this config to compile directly
  "include": [], // Let references handle includes
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    // You can put common options here that you want both app and node to inherit
    // if they don't override them. But often, it's cleaner to keep them separate.
    // For example:
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true // Often useful globally
  }
}